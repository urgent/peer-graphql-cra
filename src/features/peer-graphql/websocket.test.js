import { task } from "fp-ts/lib/Task"

const response = {
    message: "response"
}

const config = {
    message: "config"
}

const request = {
    message: "request"
}

const output = ['task', 'state']

